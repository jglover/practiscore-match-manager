$(function () {
  //
  //  Everything is handled in manage/progress.js, but we need a place holder
  //  file to prevent getting a 404 error. The menu structure expects there
  //  to be a kiosk/progress.js since we're going to that URL.
  //
});
