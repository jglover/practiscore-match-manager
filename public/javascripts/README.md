These contain the JavaScript sources for the client-side code. The directory
path correspond to the URL path
  config
  edit
  file
  kiosk
  manage
  pmelib
  score
  utils

All the client-side libraries, like JQuery, JQuery-UI, Lodash, tablesorter, etc.
  libs
